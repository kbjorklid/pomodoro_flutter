/// Filter for completed/incomplete sessions
enum CompletionStatus {
  /// Only completed sessions
  completed,
  
  /// Only incomplete sessions
  incomplete,
  
  /// Both completed and incomplete sessions
  any,
}
